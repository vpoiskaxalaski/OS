#include "stdafx.h"

//	c:\\Test1\\file1.txt
//	c:\\Test1\\

int _tmain(int argc, _TCHAR* argv[])//смена каталога ....
{
	TCHAR Buffer[256];
	TCHAR Buffer2[] = TEXT("C:\\Test1");
	DWORD dwResult = GetCurrentDirectory(256, Buffer);
	wcout << "Dir: " << Buffer << '\n';
	BOOL b = SetCurrentDirectory(Buffer2);

	dwResult = GetCurrentDirectory(256, Buffer);
	wcout << "Dir: " << Buffer << '\n';
	getchar();

	return 0;
}